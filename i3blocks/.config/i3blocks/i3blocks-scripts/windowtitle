#!/bin/sh

# Print the currently focused window's title for i3blocks

# Query the focused container
title=$(i3-msg -t get_tree \
    | jq -r '.. | objects | select(.focused? == true).name // empty')

# Fallback to empty string
echo "${title:-}"

